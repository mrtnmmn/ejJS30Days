
let date = {
    day: getDate(),
    month: getmonth(),
    year: getYear()
} 
let text = date.day = '/' + date.month + '/' + date.year

console.log(text)

if (date.month === 1 || date.month === 12) {
    if (month === 1 && (date.day > 1 || date.day < 10)) {

    }
    if (month === 12) {
        
    }
}